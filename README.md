# Section-7-Simons-Algorithm
 Simon's Algorithm aims to find a secret bitstring s by figuring out whether the function assigns identical outputs to pairs of inputs that differ exactly by s. Where the function is of the form f(x) = x XOR s
